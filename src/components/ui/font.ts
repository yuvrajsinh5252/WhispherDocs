import { Poppins } from "next/font/google";
import { Roboto } from "next/font/google";

export const poppins = Poppins({weight: "400", subsets: ["latin-ext"]});
export const roboto = Roboto({weight: "400", subsets: ["latin-ext"]});